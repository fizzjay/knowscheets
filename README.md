# Cheat solution ideas:
### terrain
Check if player position is below map position, if true, teleport player back.
### Any grab all/far grabbing script ('grabing by name', 'raycast grab', 'puppeteer')
Check distance between hand and object that is being grabbed, if distance is too long, cancel grab.
Additionally if hand is grabbing too many different items at an impossible speed, kick (or ban) from server.
### 
